// Defining outer-most store objects so we can use namespaced module approach

const state = () => ({})

const mutations = {}

const actions = {}

const getters = {}

export {
  state,
  mutations,
  actions,
  getters
}
